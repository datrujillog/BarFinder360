{
  "name": "users",
  "version": "1.0.0",
  "description": "BarFinder360",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "npx prisma migrate deploy && npx prisma generate && node ./src/app.js",
    "dev": "npx prisma migrate dev --name initial && node ./src/app.js",
    "start": "npx prisma migrate deploy && npx prisma generate && node ./src/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.11.0",
    "axios": "^1.6.8",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^3.1.0",
    "prisma": "^5.11.0"
  }
}
